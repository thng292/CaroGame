#pragma once

namespace CornersHandler {

	// Handles the game board container's corners
	void FixBoardCorners(short x, short y);

	// Handles the status bar container's corners
	void FixStatusBarCorners(short x, short y);

	// Handles the player info container's corners
	void FixPlayerInfoCorners(short x, short y);
}